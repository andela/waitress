import { updateUserInfo, refreshSlackUser } from './dashboard.js';

window.updateUserInfo = updateUserInfo
window.refreshSlackUser = refreshSlackUser

console.log(window.updateUserInfo)
// listeners

